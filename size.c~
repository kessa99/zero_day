#include <stdio.h>

/**
 * main - printt the size
 *
 * Return: 0 (Sucess)
*/

int main(void)
{
	  
	printf("%lu\n", sizeof(int));
	printf("%zu\n", &sizeof(char));
	printf("%zu\n", &sizeof(float));
	printf("%zu\n", &sizeof(double));
	printf("%zu\n", &sizeof(long));

	return (0);
}
